# SPRINT-020 Merge Notes

## Planning contract and inputs

Source: [EPIC-19](../../tickets/19-composer-attribute-adjustments/EPIC.md), its eight
existing tickets, and the accepted
[design brief](../../../compendium/attribute-adjustments.md).

The user explicitly authorized noninteractive ticket-burn planning, routine
interview omission, recorded assumptions, and automatic approval of an executable,
internally consistent sprint. No high-risk architecture choice requires a new
interview. This run changes planning records only and makes no commit. The earlier
EPIC-019 backlog-preparation instruction not to allocate a sprint applied to that
completed setup, not this runner-requested plan.

All three Codex CLI draft lanes completed successfully at xhigh effort using the
same installed command family and existing auth. No model fallback, retry, or
external model CLI was needed. Slow lanes were allowed to finish. Commands and
baseline catalog/source/code observations are recorded in
[planning evidence](SPRINT-020-PLANNING-EVIDENCE.md).

## Draft comparison

| Draft                                        | Strongest contribution                                                                                                                                                                           | Synthesis treatment                                                                                                                                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [GPT-6 Astra](SPRINT-020-GPT6ASTRA-DRAFT.md) | Detailed executable state/migration contract, contribution-scoped uncertainty, explicit hidden-command facts, non-square rune images, strict browser/file evidence, and concrete consumer audit. | Use its execution gates and uncertainty model as the main structure; simplify state into one nullable profile and keep every external format boundary explicit.                                           |
| [GPT-5.6 Sol](SPRINT-020-GPT56SOL-DRAFT.md)  | Clear nullable profile, per-contribution return-to-inherited action, outer collapsed count, actual constructor audit, and exact generated-manifest allowlist.                                    | Accept these refinements; reject all-gear reset for unrelated legacy operations, uncertainty presented as a definite zero, unnecessary comparison UI work, and execution rewriting the planning manifest. |
| [GPT-5.5](SPRINT-020-GPT55-DRAFT.md)         | Straightforward state → projection → assets → controls → interoperability sequencing and broad preservation cases.                                                                               | Keep the ordered delivery shape. Correct its title-ranks-in-game-code statement, missing 900px coverage, permissive optional effect parameters, and mistyped/unverified file names.                       |

## Selected defaults before cross-review

- One nullable `Build.attributeAdjustments` profile at Build schema v3. Strict,
  distinct inherit/None/selection representation; explicit self on/off and a
  discriminated external Refrain strength. No automatic values or derived ranks
  are persisted. Existing storage key and outer envelopes stay unchanged.
- Unknown evidence is preserved and scoped to the contributions it could affect;
  no unrelated rank is disabled and no uncertain total becomes a trustworthy zero.
  Explicit overrides replace only their addressed inherited source.
- Hidden armor mutations pass catalog-derived old/new target facts through command
  constructors. No-op edits preserve overrides; reset clears only actually
  addressed contributions. Insignia/weapon edits preserve compact gear/effects.
- A small return-to-inherited action belongs in the existing contribution controls
  or breakdown, preserving the four rune segments and headgear Clear semantics.
- Preview capping remains separate from purchased-rank validation, permanent
  equipment, title ranks, game-code projection, and raw-overlay fingerprints.
- Real icons, both themes, all required widths, zoom, keyboard/touch, persistence,
  and disposable-folder evidence remain mandatory implementation gates.

## Local file-level feasibility findings

The source audit is part of synthesis, not an additional model critique artifact.

- Both explicit Build cloners, fresh object construction in `validateBuild`, and
  constructor literals need edits. Include `library-selectors.ts` empty previews
  and comparison/copy internals in the audit without expanding mounted UI.
- Shared rank projection must feed both focused rows and `selectSkillDisplay`.
  Missing-rank support already exists in `src/domain/skill-tooltip.ts`; omit truly
  unresolved rank keys rather than substitute zero. Preserve a known zero for
  unallocated, definitely unavailable/off-profession catalog browsing contexts.
- Both inline/file and legacy dialog import warnings need review. Extend existing
  warning data; preserve the established dirty guard without an additional prompt.
- Native headgear radios must share one group across rows. Attribute ID zero is a
  valid ID; presence tests must not use truthiness. Remembered/unknown selections
  need removal access even if no eligible row can display their equipment group.
- Rune media has 126 unique media record IDs but only 30 file/URL/hash identities;
  do not deduplicate by media ID or reuse the skill cache's square-image filter.
- `.gitignore` currently ignores new files under `data/generated/epic-10/`.
  Execution must add only the exact rune provenance-manifest exception.
- Browser evidence under ignored `work/runs/` needs a durable summary in the
  executing sprint/tickets, identifying actual scenarios and artifact paths.
- Planning manifest `plan-EPIC-19-result.json` remains an immutable account of this
  plan. Execution writes its separate `execute-SPRINT-020-result.json`; `run.json`
  remains owned by the ticket-burn runner.

## Cross-critique assessment

All three critique lanes completed successfully at xhigh effort. Each reviewed
only the two other drafts; exactly three combined model critique files were
produced. Reviewer diversity was not reduced.

| Critique                                        | Accepted findings                                                                                                                                                                                                                                                                                                                                | Rejected or narrowed findings                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [GPT-6 Astra](SPRINT-020-GPT6ASTRA-CRITIQUE.md) | Strict v3 topology and canonical ordering; one Build-plus-catalog projection; orphaned override recovery; missing-rank rather than false zero; inherited reset; actual file mode context; production-preview browser evidence; achievable staged asset publication; avoid redundant full-suite runs.                                             | Preserve the accepted conservative scoped uncertainty for unknown inherited gear instead of treating it as a definite zero. Keep the requested exact planning-result path for traceability, while execution uses its own runner-requested result path.                                                                                                    |
| [GPT-5.6 Sol](SPRINT-020-GPT56SOL-CRITIQUE.md)  | Shared source-eligibility primitives and collision-safe slot facts; match `templateId` separately from catalog `id`; explicit one-context data flow; unique reverse rune-tier lookup; existing ShareControls/party multi-code omission surfaces; maximum nested transfer fixtures; fail closed on asset hash mismatch; tracked evidence summary. | Reject automatic metadata refresh: it would detach bytes from the promoted catalog and broaden scope. Do not split this plan solely on an unquantified capacity concern; all eight groomed tickets have executable dependencies and there is no imposed time/effort cap. Execution must leave unmet gates open, never cut acceptance to claim completion. |
| [GPT-5.5](SPRINT-020-GPT55-CRITIQUE.md)         | Code-backed BW-1901 contracts before Build migration; one nullable profile; scoped legacy uncertainty; no-loadout guards; asset branch after BW-1901; ticket-specific gates; focused durable docs; separate execution records.                                                                                                                   | Use the existing phase checklists and explicit per-ticket gates instead of duplicating the entire checklist under a second DoD hierarchy. The precise planning manifest path is explicitly required by the user, so it remains recorded.                                                                                                                  |

The final UI contract lets an inactive remembered on request remain checked and
editable, with an explicit inactive reason; count and ranks use eligible activity.
This avoids a checkbox that cannot be switched off while its source is absent.

The synthesis uses Astra's phase/gate structure, Sol's aggregate and recovery
affordances, GPT-5.5's straightforward delivery order, and concrete findings from
all reviews. It is not a concatenation of the drafts. File lists distinguish
required edits from regression targets.

Additional parent verification confirmed `ShareControls.tsx` has equipment/title
omission notices, `party-sharing.ts` has `omitted` and `lossyCount` fields,
`BuildValidationContext` exposes collision/slot facts, the app separately renders
its pinned tooltip, and build-set/party transfers cap at 240,000 bytes with 16-entry
build sets. These findings are incorporated into BW-1902, BW-1903, and BW-1907.

## Interview and approval

Routine interview skipped under the user's explicit automation policy. Existing
UI, asset authorization, scope, storage ownership, effect inference, and game-file
decisions were not reopened. The final [SPRINT-020](../SPRINT-020.md) is auto-approved under that contract after
internal consistency, executable phase/gate, and planning integrity review.

## Final planning validation

- The actual ticket-burn parser accepts the final sprint, epic, and all eight ticket
  records. The original dependency DAG is preserved; all ten prerequisite epics
  are done. BW-1901 is ready; BW-1902 through BW-1908 remain backlog with
  `planned_sprint: SPRINT-020`. No implementation acceptance is marked done.
- [The ledger](../ledger.tsv) has one new planned 020 row; all nineteen historical
  rows are byte-for-byte unchanged.
- [The requested JSON result](../../runs/ticket-burn/EPIC-19/20260914T004651Z/plan-EPIC-19-result.json)
  has `status: planned`, the exact source/sprint/path fields, null blocked reason,
  and nine recorded assumptions. Execution does not overwrite this result.
- All three independent drafts and exactly three combined cross-critiques are
  present; all six model CLI runs exited successfully. No reduced-diversity fallback.
- Relative markdown links, required sprint sections, eight phase gates, single
  canonical profile vocabulary, browser widths/zoom, and separate execution-result
  ownership pass planning checks. New/untracked markdown was included in the
  whitespace review.
- Targeted Prettier formatting/checks and `git diff --check` pass. Git scope checks
  show only the requested planning records/artifacts. HEAD remains `41b5e41`;
  no product code, promoted catalogs, or commits were changed.
- Available Chrome/Firefox/Safari applications and validation tools were detected.
  No implementation tests, image acquisition, or actual browser acceptance were
  performed or claimed; those remain unchecked execution gates.

The final plan, ledger, source tickets, and planned result agree. The runner's
`run.json` was not edited by this planner.
