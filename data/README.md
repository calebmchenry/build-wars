# Data Artifacts

This directory reserves stable locations for future data work:

- `source-snapshots/` stores raw source captures when a source policy exists.
- `generated/` stores normalized artifacts produced by data scripts.
- `qa/` stores generated validation and coverage reports.

Only policy README files belong in these directories during the foundation sprint.
